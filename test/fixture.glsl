precision mediump float;

struct Thing {
  vec4 thing1;
  vec4 thing2;
};

Thing hello;

void main() {
  for (int loop1 = 0; loop1 < 5; loop1++) {
    first[i];

    do {
      second;

      for (int loop2 = 0; loop2 < 10; loop2++) {
        third[j] = fourth[i]; if (fifth[0] > 10) {
          sixth[j] -= 2;
        }
      }
    } while(seventh);
  }

  int eighth = 5;
  do {
    ninth[eighth] = 0;
  } while(eighth--)

  int tenth = 0;
  while (tenth > 0) {
    tenth -= 1;
  }

  gl_FragColor = vec4(1.0);
}
